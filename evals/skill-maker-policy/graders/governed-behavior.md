# Evidence-based policy grader

Inspect the complete actual host trace, source-policy revisions and resulting bytes against the selected case in the parent README. The prompt alone, intended output, source assertions and self-reported success are not evidence of execution.

For P01/P02/P05/P06 require observed policy reads at the relevant invocation/resume boundary and correct observed artifact content. Require unchanged generated bundle hashes across central-policy changes. For P03/P04/P10 require the specific unresolved reference/conflicting sources/ambiguity and preserved publication targets, not any generic refusal. P04 must identify both requirements; generic safety language is insufficient.

Inspect every generated bundle file, not only SKILL.md, for private policy content, private markers, originating studio identity and absolute policy locators. Generic dynamic references to the current studio's STUDIO.md are expected. Require live-reference instructions rather than a snapshot of the synthetic prefix rule. Treat fixture markers in a legitimate operator source read as allowed evidence, not a public output leak.

For setup, compare the entire before/after set with the approved preparation. Existing adoption never creates a copy, default adoption uses the resolved custom root, decline creates none, and unrelated edits preserve prior configuration. No blanket file/directory ignores. Record actual source revision/digest, host, case, attempt, evidence paths, passed/failed/not-run status and limitation. Do not claim discovery from explicit file loading or policy execution from file generation.
