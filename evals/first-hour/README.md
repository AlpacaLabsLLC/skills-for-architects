# Final invocation acceptance

Run these cases only after the full R4 implementation, package and documentation are complete. Map the actual product/version/session to Mac or Windows and CLI or desktop; a product name alone does not prove that combination. Record candidate/package identity, actual inputs, transcript/evidence locators, operations/tool creation results, expected result and observed pass/fail/unavailable for each case. Repeat on every claimed route, with real fresh setup and approved copies where writes are needed. No result is recorded here merely because the instructions exist.

The cases cover simple work, source navigation versus interpretation, plan approval states, resumed authorization, actual team creation and state-aware setup. No mandatory Norma invocation metric, branding test or artificial timing threshold applies. Use existing engineering evidence for unchanged contracts; this matrix is actual host behavior acceptance, performed last.
