{
  "path": "skills/epd-research/SKILL.md",
  "responsibility": "Search for EPDs by product category, CSI division, or material type \u2014 finds EPDs from EC3, program operator registries, and manufacturer sites. Use when the user asks to \"find EPDs for...\", locate declarations for a material, or source low-carbon product documentation.",
  "inputs": "Supplied task and selected source scope; exact destination only for requested persistence or application work. Load project context only when the task needs project records.",
  "owned_records": [
    "epd-library.csv"
  ],
  "non_owned_records": [
    "PROJECT.md",
    "STUDIO.md",
    "TASKS.csv",
    "TIME.csv",
    "proposals/",
    "agreement/",
    "INVOICES.csv",
    "ffe/items-and-schedules",
    "product-library.csv",
    "ffe/jobs/input-manifests"
  ],
  "delegation": "inherit-scope-target-permissions-and-evidence",
  "modes": [
    {
      "mode": "research",
      "when": "Only for substantive findings requiring applicable originals not already supplied; metadata-only source directories need no content retrieval."
    },
    {
      "mode": "record-write",
      "when": "Only for explicit EPD library persistence under its native 42-column owner; normal selected-row save validates and appends one complete batch.",
      "required": [
        "file.read",
        "file.write"
      ],
      "optional": [
        "process.run"
      ]
    },
    {
      "mode": "delegate",
      "when": "Only for requested extraction/research owner coordination or project facts/decisions and registered documents; inherit exact targets, source evidence and existing authorization."
    },
    {
      "mode": "file-output",
      "when": "Only for requested rows/reports at an authorized destination; inline extraction/research needs no write.",
      "required": [
        "file.read",
        "file.write"
      ],
      "optional": [
        "process.run"
      ]
    },
    {
      "mode": "file-read",
      "when": "When reading authorized supplied source documents or retained task evidence.",
      "required": [
        "file.read"
      ],
      "optional": [
        "pdf.read"
      ]
    },
    {
      "mode": "record-read",
      "when": "Only when consuming or explicitly saving an existing selected EPD library; native validation changes no records.",
      "required": [
        "file.read"
      ],
      "optional": [
        "process.run"
      ]
    }
  ],
  "execution": {
    "model": "harness-native",
    "contract": "tools/workspace/epd-library-contract.md",
    "operations": [
      "product_library.validate",
      "product_library.append"
    ]
  }
}
