# Drawing quantity extraction

Count supplied drawing instances with document/page/position evidence and explicit coverage gaps.
Use `/as:drawing-quantity-extract` or `$drawing-quantity-extract`. Host PDF/OCR/visual access is
required; output is a derived ledger, not an adopted schedule or certified takeoff.
