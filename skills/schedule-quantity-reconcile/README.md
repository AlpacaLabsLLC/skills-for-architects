# Schedule quantity reconciliation

Compare two scoped quantity snapshots without choosing an authoritative value or changing records.
Use `/as:schedule-quantity-reconcile` or `$schedule-quantity-reconcile`. Extraction remains separate;
missing, unknown, different-scope and conflicting evidence stay explicit.
