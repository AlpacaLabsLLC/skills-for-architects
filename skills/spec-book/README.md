# Specification book

Assemble the complete agreed FF&E sheet set and optional combined PDF. Reuse verified unchanged sheets after interruption, invalidate corrections, and report exact missing/failed items without erasing prior outputs.

Use `$spec-book` on Codex or `/as:spec-book` on Claude Code. Composes [product-cut-sheet](../product-cut-sheet/README.md); rendering and application access come from the host. See [output contracts](../../tools/renderers/README.md).
