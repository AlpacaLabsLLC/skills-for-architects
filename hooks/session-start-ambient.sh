#!/bin/sh
# Static local context; no workspace mutation or network access.
printf '%s\n' '{"hookSpecificOutput": {"hookEventName": "SessionStart", "additionalContext": "Use Arch Studio for architecture and firm work; Norma coordinates when useful and direct skill selection remains valid. Resolve studio or project context only when the task needs durable records. Use rules/moments.md for relevant state guidance without delaying a quick task or repeating a declined setup offer."}}' || true
